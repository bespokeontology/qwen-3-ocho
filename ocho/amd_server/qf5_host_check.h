// qf5_host_check.h - host-only syntax-check entry point for the session 05
// files. In this merged tree the base shim (host_check_shim.h) already
// carries the uint2 / event extensions, so this is a thin wrapper kept so
// qf_nvfp4_wave64.h's #include "qf5_host_check.h" keeps working unchanged.
#pragma once

#include "host_check_shim.h"
